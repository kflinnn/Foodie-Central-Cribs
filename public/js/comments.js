const CreateComments = async (event) => {
    event.preventDefault();

    const name = document.querySelector('').value.trim();
    const age = document.querySelector('').value.trim();
    const height = document.querySelector('').value.trim();
    const weght = document.querySelector('').value.trim();


}

document
  .querySelector('')
  .addEventListener('submit', CreateComments);
